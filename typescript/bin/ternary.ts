const newValue = 1;
const previousValue = 0;

const hasIncreased = newValue > previousValue;
const hasDecreased = newValue < previousValue;

console.log('hasIncreased: ' + hasIncreased);
console.log('hasDecreased: ' + hasDecreased);
